using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pebble1Script : MonoBehaviour
{
    //public GameObject MancalaPebble1;
    // Start is called before the first frame update
    void Start()
    {
        //this.gameObject.GetComponent<SpriteRenderer>().sprite = MancalaPebble1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
